# This is an Elastic Beanstalk configuration file
# It runs commands *during* deployment, before the app starts

container_commands:
  01_migrate:
    command: "python run_migrations.py"
    leader_only: true